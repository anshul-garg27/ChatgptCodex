# Simple Calculator

This repository contains a small command-line calculator application written in Python.

## Usage

Run the calculator interactively:

```bash
python3 calculator.py
```

Follow the prompts to choose an operation and enter numbers.

## Running Tests

Use `pytest` to run the unit tests:

```bash
pytest
```
